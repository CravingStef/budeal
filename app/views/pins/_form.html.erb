<%= simple_form_for(@pin, html: { class: "form-horizontal"}) do |f| %>
  <%= f.error_notification %>

  <%= f.input :description, as: :text, input_html: { rows: "2" } %>
	<%= f.input :price, :wrapper => :append, :class => "inline" do %>
   <%= f.input_field :price, :class => "input-mini" %>
    <%= content_tag :span, "€", :class => "add-on abbn" %>
   <% end %>

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
